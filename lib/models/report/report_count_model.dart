class Counts {
  int invoiceCount;
  double totalAmount;
  double paidAmount;
  double dueAmount;
  double overdueAmount;

  Counts(this.invoiceCount, this.totalAmount, this.paidAmount, this.dueAmount,
      this.overdueAmount);
}